/**
 * setup.js
 * Fichier de configuration global pour Vitest.
 * Importe les matchers jest-dom pour enrichir les assertions :
 *   toBeInTheDocument(), toHaveTextContent(), toBeDisabled(), etc.
 */
import '@testing-library/jest-dom'
